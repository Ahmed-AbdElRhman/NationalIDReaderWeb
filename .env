#APP Configuration
MAX_SCANS='10'
SUBSCRIPTION_EXPIRY_DAYS='10'
SUBSCRIPTION_SECRET_KEY = 65131630b383c5c9b6ca42459a187de479596ae8ec485d84a672a53d19f03cb1
#Server Configuration
# SERVER_URL=http://127.0.0.1:5000/process  # URL of the server
# SERVER_PORT=5000                  # Port on which the server runs
# Database Configuration
# DATABASE_URL=mysql+pymysql://username:password@localhost/OCRDB_dev
DATABASE_URL='DRIVER={SQL Server};SERVER=DESKTOP-TRPSBPE\SQLEXPRESS;DATABASE=OCRDB_dev;'
DB_NAME=instance/OCRDB.db        # Name of the database file
DB_USER=                         # Database user 
DB_PASSWORD=                     # Database password 
DB_HOST=localhost                # Database host 
DB_PORT=3306                     # Database port 
# Logging Configuration
LOG_LEVEL=DEBUG                  # DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_FILE=logs/IDReader.log      # Path to log file
MAX_LOG_SIZE_MB=10              # Max size per log file in MB
LOG_BACKUP_COUNT=10             # Number of backup logs to keep
LOG_ENABLE_CONSOLE=true         # Enable console logging
LOG_ENABLE_FILE=true            # Enable file logging
APP_NAME=IDReader               # Application name for logs
# Gemini Configuration
GEMINI_API_KEY=AIzaSyAik3z3ti8T1naBujWuN24MDZfTomMwzhQ  # Gemini API key
GEMINI_MODEL=gemini-2.0-flash-001  # Default Gemini model to use
# GEMINI_OCR_PROMPT='This is an Arabic National ID card, which contains the person fisrt name then parent full name then adress then the national ID, and other data. Act as a highly accurate and specialized Arabic Optical Character Recognition (OCR) system, so please extract all Arabic characters/numbers from the image, be proficient in interpreting Arabic National ID cards, and send the response as a static JSON form containing {firstname:"",parentfullname:"",address:"",nationalID:"}. You can also predict the word that was not read correctly.make it fast an acurate and also make sure to return all data in English'
GEMINI_OCR_PROMPT='This is an Arabic National ID card, which contains the person fisrt name then parent full name then adress then the national ID, and other data. Act as a highly accurate and specialized Arabic Optical Character Recognition (OCR) system, so please extract all Arabic characters/numbers from the image, be proficient in interpreting Arabic National ID cards.The extracted information should be returned as a single JSON object with the following nested keys: "firstname", "parentfullname", "NationalID", and "address". Each of these should contain a nested object with "arabic" and "english" string fields.You can also predict the word that was not read correctly.make it fast an acurate.'
# Model Configuration
PROCESSED_IMAGE_TYPE=.jpg  # Image type for processed images